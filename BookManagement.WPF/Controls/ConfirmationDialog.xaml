<UserControl x:Class="BookManagement.Controls.ConfirmationDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BookManagement.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="600"
             >
    <!-- Semi-transparent overlay background -->
    <Grid Style="{StaticResource DialogOverlayStyle}">
        <!-- Dialog Card -->
        <Border Style="{StaticResource DialogCardStyle}">
            <StackPanel>
                <!-- Warning / Info Icon -->
                <Border Style="{StaticResource DialogIconCircleStyle}" Background="{DynamicResource WarningLightBrush}">
                    <Path Data="{StaticResource IconShield}" 
                          Fill="{DynamicResource WarningBrush}" 
                          Width="24" Height="24" 
                          Stretch="Uniform" 
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center"/>
                </Border>

                <!-- Title -->
                <TextBlock 
                           Style="{StaticResource TextBlockSmallHeader}" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,8"/>

                <!-- Message -->
                <TextBlock 
                           Style="{StaticResource TextBlockNormal}" 
                           TextAlignment="Center" 
                           TextWrapping="Wrap" 
                           HorizontalAlignment="Center" 
                           Margin="0,0,0,24"/>

                <!-- Action Buttons -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Cancel -->
                    <Button Grid.Column="0" 
                            Style="{StaticResource SecondaryButton}" 
                            Content="Hủy" 
                            Click="CancelButton_Click"/>

                    <!-- Confirm -->
                    <Button Grid.Column="2" 
                            Style="{StaticResource PrimaryButton}" 
                            Content="Xác nhận" 
                            Click="ConfirmButton_Click"/>
                </Grid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
