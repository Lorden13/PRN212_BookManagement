<UserControl x:Class="BookManagement.Views.Admin.AdminReaderDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:BookManagement.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- PageHeader / Back Button -->
            <RowDefinition Height="*"/>    <!-- TabControl -->
        </Grid.RowDefinitions>

        <!-- Back Button Toolbar -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Button Style="{StaticResource OutlinedButton}" Content="← Quay lại danh sách" Height="36" HorizontalAlignment="Left" Command="{Binding BackCommand}"/>
        </Grid>

        <!-- TabControl -->
        <Border Grid.Row="1" Style="{StaticResource BaseCardStyle}" Padding="20">
            <TabControl Style="{StaticResource ModernTabControl}">
                <!-- TAB 1: INFORMATION -->
                <TabItem Header="Thông tin chung">
                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/> <!-- Avatar Area -->
                            <ColumnDefinition Width="*"/>   <!-- Info Area -->
                        </Grid.ColumnDefinitions>

                        <!-- Avatar Card -->
                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <controls:AvatarCard Width="100" Height="100" DataContext="{Binding Reader}"/>
                            <TextBlock Text="{Binding Reader.Name}" Style="{StaticResource TextBlockBold}" FontSize="16" HorizontalAlignment="Center" Margin="0,12,0,0" TextAlignment="Center"/>
                            <controls:StatusBadge Content="{Binding Reader.Status}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                        </StackPanel>

                        <!-- Info Grid -->
                        <Grid Grid.Column="1" Margin="30,0,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Mã độc giả:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Reader.Id, StringFormat='#USR{0:D5}'}" Style="{StaticResource TextBlockBold}"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Reader.Email}" Style="{StaticResource TextBlockNormal}"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Số điện thoại:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding Reader.Phone}" Style="{StaticResource TextBlockNormal}"/>

                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Địa chỉ:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock Grid.Row="6" Grid.Column="1" Text="{Binding Reader.Address}" Style="{StaticResource TextBlockNormal}" TextWrapping="Wrap"/>

                            <TextBlock Grid.Row="8" Grid.Column="0" Text="Ngày đăng ký:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock Grid.Row="8" Grid.Column="1" Text="{Binding Reader.JoinedDate}" Style="{StaticResource TextBlockNormal}"/>

                            <TextBlock Grid.Row="10" Grid.Column="0" Text="Tổng đơn mua:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock Grid.Row="10" Grid.Column="1" Text="{Binding TotalPurchases, StringFormat='{}{0} đơn hàng'}" Style="{StaticResource TextBlockBold}" Foreground="{DynamicResource PrimaryPurpleBrush}"/>

                            <TextBlock Grid.Row="12" Grid.Column="0" Text="Tổng tiền chi tiêu:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock Grid.Row="12" Grid.Column="1" Text="{Binding TotalSpending, StringFormat='${0:F2}'}" Style="{StaticResource TextBlockBold}" Foreground="{DynamicResource SuccessBrush}"/>
                        </Grid>
                    </Grid>
                </TabItem>

                <!-- TAB 2: PURCHASE HISTORY -->
                <TabItem Header="Lịch sử mua sách">
                    <Grid Margin="0,20,0,0">
                        <DataGrid ItemsSource="{Binding PurchaseHistory}" Style="{StaticResource ModernDataGrid}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã giao dịch" Binding="{Binding Id, StringFormat='#TXN{0:D5}'}" Width="1.5*"/>
                                <DataGridTextColumn Header="Tên tác phẩm" Binding="{Binding BookTitle}" Width="3*"/>
                                <DataGridTextColumn Header="Số tiền" Binding="{Binding Price, StringFormat='${0:F2}'}" Width="1.5*"/>
                                <DataGridTextColumn Header="Ngày giao dịch" Binding="{Binding PurchaseDate}" Width="2*"/>
                                <DataGridTemplateColumn Header="Trạng thái" Width="1.5*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <controls:StatusBadge Content="{Binding Status}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

                <!-- TAB 3: FAVORITES -->
                <TabItem Header="Sách yêu thích">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,20,0,0">
                        <ItemsControl ItemsSource="{Binding FavoriteBooks}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <controls:BookCard Margin="8"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>

                <!-- TAB 4: ACTIVITIES TIMELINE -->
                <TabItem Header="Hoạt động gần đây">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,20,0,0">
                        <ItemsControl ItemsSource="{Binding Activities}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="10" Height="10" CornerRadius="5" Background="{DynamicResource PrimaryPurpleBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Message}" Style="{StaticResource TextBlockNormal}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Time}" Style="{StaticResource TextBlockMuted}" FontSize="12" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>
