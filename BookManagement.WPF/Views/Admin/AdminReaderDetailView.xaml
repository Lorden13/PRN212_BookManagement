<UserControl x:Class="BookManagement.Views.Admin.AdminReaderDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:BookManagement.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- PageHeader / Back Button -->
            <RowDefinition Height="*"/>    <!-- TabControl -->
        </Grid.RowDefinitions>

        <!-- Back Button Toolbar -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Button Style="{StaticResource OutlinedButton}" Click="BtnBack_Click" Content="← Quay lại danh sách" Height="36" HorizontalAlignment="Left"/>
        </Grid>

        <!-- TabControl -->
        <Border Grid.Row="1" Style="{StaticResource BaseCardStyle}" Padding="20">
            <!--<TabControl Style="{StaticResource ModernTabControl}">-->
            <TabControl Style="{x:Null}">
                <!-- TAB 1: INFORMATION -->
                <TabItem Header="Thông tin chung">
                    <Grid Margin="0,20,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/> <!-- Avatar Area -->
                            <ColumnDefinition Width="*"/>   <!-- Info Area -->
                        </Grid.ColumnDefinitions>

                        <!-- Avatar Card -->
                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <Border Width="100" Height="100" CornerRadius="50" Background="{DynamicResource PrimaryPurpleLightBrush}" BorderBrush="{DynamicResource PrimaryPurpleActiveBrush}" BorderThickness="1.5" VerticalAlignment="Center">
                                <TextBlock x:Name="txtReaderNameLetter" Text="R" Foreground="{DynamicResource PrimaryPurpleBrush}" FontWeight="Bold" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center" FontFamily="{DynamicResource MainFontFamily}"/>
                            </Border>
                            <TextBlock x:Name="txtReaderNameHeader" Text="Reader Name" Style="{StaticResource TextBlockBold}" FontSize="16" HorizontalAlignment="Center" Margin="0,12,0,0" TextAlignment="Center"/>
                            <controls:StatusBadge x:Name="txtReaderStatus" Content="Active" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                        </StackPanel>

                        <!-- Info Grid -->
                        <Grid Grid.Column="1" Margin="30,0,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="12"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Mã độc giả:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock x:Name="txtReaderId" Grid.Row="0" Grid.Column="1" Text="#USR00001" Style="{StaticResource TextBlockBold}"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Email:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock x:Name="txtReaderEmail" Grid.Row="2" Grid.Column="1" Text="reader@email.com" Style="{StaticResource TextBlockNormal}"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Số điện thoại:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock x:Name="txtReaderPhone" Grid.Row="4" Grid.Column="1" Text="0123456789" Style="{StaticResource TextBlockNormal}"/>

                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Vai trò:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock x:Name="txtReaderRole" Grid.Row="6" Grid.Column="1" Text="Reader" Style="{StaticResource TextBlockNormal}"/>

                            <TextBlock Grid.Row="8" Grid.Column="0" Text="Tổng đơn mua:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock x:Name="txtTotalPurchases" Grid.Row="8" Grid.Column="1" Text="0 đơn hàng" Style="{StaticResource TextBlockBold}" Foreground="{DynamicResource PrimaryPurpleBrush}"/>

                            <TextBlock Grid.Row="10" Grid.Column="0" Text="Tổng tiền chi tiêu:" Style="{StaticResource TextBlockMuted}"/>
                            <TextBlock x:Name="txtTotalSpending" Grid.Row="10" Grid.Column="1" Text="$0.00" Style="{StaticResource TextBlockBold}" Foreground="{DynamicResource SuccessBrush}"/>
                        </Grid>
                    </Grid>
                </TabItem>

                <!-- TAB 2: PURCHASE HISTORY -->
                <TabItem Header="Lịch sử mua sách">
                    <Grid Margin="0,20,0,0">
                        <DataGrid x:Name="dgPurchases" Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mã giao dịch" Binding="{Binding Id}" Width="1.5*"/>
                                <DataGridTextColumn Header="Tên tác phẩm" Binding="{Binding BookTitle}" Width="3*"/>
                                <DataGridTextColumn Header="Số tiền" Binding="{Binding Price, StringFormat='${0:F2}'}" Width="1.5*"/>
                                <DataGridTextColumn Header="Ngày giao dịch" Binding="{Binding PurchaseDate}" Width="2*"/>
                                <DataGridTemplateColumn Header="Trạng thái" Width="1.5*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <controls:StatusBadge Content="{Binding Status}"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>

                <!-- TAB 3: FAVORITES -->
                <TabItem Header="Sách yêu thích">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,20,0,0">
                        <ItemsControl x:Name="icFavorites">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <controls:BookCard Margin="8"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>

                <!-- TAB 4: ACTIVITIES TIMELINE -->
                <TabItem Header="Hoạt động gần đây">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,20,0,0">
                        <ItemsControl x:Name="icActivities">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,16">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Width="10" Height="10" CornerRadius="5" Background="{DynamicResource PrimaryPurpleBrush}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Message}" Style="{StaticResource TextBlockNormal}" FontWeight="SemiBold"/>
                                            <TextBlock Text="{Binding Time}" Style="{StaticResource TextBlockMuted}" FontSize="12" Margin="0,4,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </Border>
    </Grid>
</UserControl>
