<UserControl x:Class="BookManagement.Views.Admin.AdminHomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:BookManagement.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="1200">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Stat Cards -->
                <RowDefinition Height="Auto"/> <!-- Widgets Grid -->
                <RowDefinition Height="*"/>    <!-- Logs & Pending Books -->
            </Grid.RowDefinitions>

            <!-- Stat Cards Row -->
            <UniformGrid Grid.Row="0" Columns="4" Margin="-8,0,-8,20">
                <controls:StatisticCard x:Name="cardTotalUsers" Title="Tổng số tài khoản" Value="0" IconKey="IconUsers" TrendText="Độc giả + Tác giả + Admin"/>
                <controls:StatisticCard x:Name="cardTotalReaders" Title="Tổng số độc giả" Value="0" IconKey="IconUser" TrendText="Bạn đọc đã đăng ký"/>
                <controls:StatisticCard x:Name="cardTotalAuthors" Title="Tổng số tác giả" Value="0" IconKey="IconPen" TrendText="Nhà sáng tác nội dung"/>
                <controls:StatisticCard x:Name="cardTotalBooks" Title="Tổng số sách" Value="0" IconKey="IconBook" TrendText="Sách trong hệ thống"/>
                <controls:StatisticCard x:Name="cardPendingBooks" Title="Yêu cầu chờ duyệt" Value="0" IconKey="IconClock" TrendText="Sách đang đợi kiểm duyệt" TrendBrush="{DynamicResource WarningBrush}"/>
                <controls:StatisticCard x:Name="cardTotalRevenue" Title="Doanh thu tích lũy" Value="$0.00" IconKey="IconCart" TrendText="Tổng giao dịch mua sách" TrendBrush="{DynamicResource SuccessBrush}"/>
                <controls:StatisticCard x:Name="cardTodaySales" Title="Doanh thu hôm nay" Value="$0.00" IconKey="IconCheck" TrendText="Doanh thu ngày hôm nay" TrendBrush="{DynamicResource InfoBrush}"/>
            </UniformGrid>

            <!-- Widgets Grid (Charts) -->
            <Grid Grid.Row="1" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <!-- Revenue Mock Chart -->
                <Border Grid.Column="0" Style="{StaticResource BaseCardStyle}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Biểu đồ Doanh thu (Mock)" Style="{StaticResource TextBlockBold}" FontSize="16" Margin="0,0,0,16"/>
                        <Grid Height="150" Margin="0,10,0,0">
                            <UniformGrid Columns="6" VerticalAlignment="Bottom" Height="120">
                                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center">
                                    <Border Width="16" Height="40" Background="{DynamicResource PrimaryPurpleHoverBrush}" CornerRadius="4,4,0,0"/>
                                    <TextBlock Text="T1" Style="{StaticResource TextBlockMuted}" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center">
                                    <Border Width="16" Height="60" Background="{DynamicResource PrimaryPurpleHoverBrush}" CornerRadius="4,4,0,0"/>
                                    <TextBlock Text="T2" Style="{StaticResource TextBlockMuted}" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center">
                                    <Border Width="16" Height="80" Background="{DynamicResource PrimaryPurpleHoverBrush}" CornerRadius="4,4,0,0"/>
                                    <TextBlock Text="T3" Style="{StaticResource TextBlockMuted}" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center">
                                    <Border Width="16" Height="50" Background="{DynamicResource PrimaryPurpleHoverBrush}" CornerRadius="4,4,0,0"/>
                                    <TextBlock Text="T4" Style="{StaticResource TextBlockMuted}" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center">
                                    <Border Width="16" Height="95" Background="{DynamicResource PrimaryPurpleBrush}" CornerRadius="4,4,0,0"/>
                                    <TextBlock Text="T5" Style="{StaticResource TextBlockMuted}" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                                <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center">
                                    <Border Width="16" Height="110" Background="{DynamicResource PrimaryPurpleDarkBrush}" CornerRadius="4,4,0,0"/>
                                    <TextBlock Text="T6" Style="{StaticResource TextBlockMuted}" FontSize="10" HorizontalAlignment="Center" Margin="0,4,0,0"/>
                                </StackPanel>
                            </UniformGrid>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Book Status Chart -->
                <Border Grid.Column="2" Style="{StaticResource BaseCardStyle}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Phân bổ trạng thái ấn phẩm" Style="{StaticResource TextBlockBold}" FontSize="16" Margin="0,0,0,16"/>
                        <StackPanel Margin="0,10,0,0">
                            <!-- Approved -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Đã duyệt" Style="{StaticResource TextBlockNormal}" VerticalAlignment="Center"/>
                                <ProgressBar x:Name="pbApprovedStatus" Grid.Column="1" Value="0" Height="10" Foreground="#10B981" Background="#F3F4F6" BorderThickness="0"/>
                                <TextBlock x:Name="lblApprovedPercent" Grid.Column="2" Text="0%" Style="{StaticResource TextBlockBold}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                            <!-- Pending -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Chờ duyệt" Style="{StaticResource TextBlockNormal}" VerticalAlignment="Center"/>
                                <ProgressBar x:Name="pbPendingStatus" Grid.Column="1" Value="0" Height="10" Foreground="#F59E0B" Background="#F3F4F6" BorderThickness="0"/>
                                <TextBlock x:Name="lblPendingPercent" Grid.Column="2" Text="0%" Style="{StaticResource TextBlockBold}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                            <!-- Rejected -->
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Từ chối" Style="{StaticResource TextBlockNormal}" VerticalAlignment="Center"/>
                                <ProgressBar x:Name="pbRejectedStatus" Grid.Column="1" Value="0" Height="10" Foreground="#EF4444" Background="#F3F4F6" BorderThickness="0"/>
                                <TextBlock x:Name="lblRejectedPercent" Grid.Column="2" Text="0%" Style="{StaticResource TextBlockBold}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Bottom Row (Logs & Lists) -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!-- Latest Pending Books -->
                <Border Grid.Column="0" Style="{StaticResource BaseCardStyle}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Đề xuất chờ kiểm duyệt gần đây" Style="{StaticResource TextBlockBold}" FontSize="16" Margin="0,0,0,16"/>
                        <DataGrid x:Name="dgPendingBooks" Style="{StaticResource ModernDataGrid}" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Tên tác phẩm" Binding="{Binding Title}" Width="2*"/>
                                <DataGridTextColumn Header="Tác giả" Binding="{Binding Author}" Width="1.5*"/>
                                <DataGridTextColumn Header="Ngày gửi" Binding="{Binding SubmittedDate}" Width="1.5*"/>
                                <DataGridTemplateColumn Header="Thao tác" Width="100">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Style="{StaticResource OutlinedButton}" Content="Xét duyệt" Height="26" Padding="8,0" Click="BtnReviewBook_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- Recent Activities timeline -->
                <Border Grid.Column="2" Style="{StaticResource BaseCardStyle}" Padding="20">
                    <StackPanel>
                        <TextBlock Text="Nhật ký hoạt động hệ thống" Style="{StaticResource TextBlockBold}" FontSize="16" Margin="0,0,0,16"/>
                        <ItemsControl x:Name="icActivities">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,0,0,12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Border Grid.Column="0" Width="10" Height="10" CornerRadius="5" Background="{DynamicResource PrimaryPurpleBrush}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                        
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="{Binding Message}" Style="{StaticResource TextBlockNormal}" TextWrapping="Wrap"/>
                                            <TextBlock Text="{Binding Time}" Style="{StaticResource TextBlockMuted}" FontSize="11" Margin="0,2,0,0"/>
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
