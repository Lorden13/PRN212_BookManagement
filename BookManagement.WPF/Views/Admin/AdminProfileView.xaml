<UserControl x:Class="BookManagement.Views.Admin.AdminProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:BookManagement.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Column 1: Profile Information -->
            <Border Grid.Column="0" Style="{StaticResource BaseCardStyle}" Padding="24">
                <StackPanel>
                    <TextBlock Text="Thông tin cá nhân Admin" Style="{StaticResource TextBlockBold}" FontSize="18" Margin="0,0,0,20"/>
                    
                    <!-- Avatar Selection -->
                    <StackPanel HorizontalAlignment="Center" Margin="0,0,0,24">
                        <controls:AvatarCard Width="80" Height="80"/>
                        <Button Style="{StaticResource OutlinedButton}" Content="Thay đổi ảnh đại diện" Height="32" Margin="0,12,0,0" Command="{Binding SelectAvatarCommand}"/>
                    </StackPanel>

                    <!-- Profile Form -->
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Họ và tên:" Style="{StaticResource TextBlockNormal}" Margin="0,0,0,6"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Tag="Họ và tên"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Email liên hệ:" Style="{StaticResource TextBlockNormal}" Margin="0,0,0,6"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Tag="Email"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="Số điện thoại:" Style="{StaticResource TextBlockNormal}" Margin="0,0,0,6"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Tag="Số điện thoại"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,20">
                        <TextBlock Text="Địa chỉ làm việc:" Style="{StaticResource TextBlockNormal}" Margin="0,0,0,6"/>
                        <TextBox Style="{StaticResource ModernTextBox}" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Tag="Địa chỉ"/>
                    </StackPanel>

                    <!-- Save Action -->
                    <Button Style="{StaticResource PrimaryButton}" Content="Lưu thông tin" Height="40" Command="{Binding SaveCommand}"/>
                </StackPanel>
            </Border>

            <!-- Column 2: Security & Password & Actions -->
            <StackPanel Grid.Column="2">
                <!-- Change Password Card -->
                <Border Style="{StaticResource BaseCardStyle}" Padding="24" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Đổi mật khẩu" Style="{StaticResource TextBlockBold}" FontSize="18" Margin="0,0,0,20"/>
                        
                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Mật khẩu hiện tại:" Style="{StaticResource TextBlockNormal}" Margin="0,0,0,6"/>
                            <PasswordBox x:Name="txtCurrentPassword" Style="{StaticResource ModernPasswordBox}" PasswordChanged="PasswordBox_PasswordChanged"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,16">
                            <TextBlock Text="Mật khẩu mới:" Style="{StaticResource TextBlockNormal}" Margin="0,0,0,6"/>
                            <PasswordBox x:Name="txtNewPassword" Style="{StaticResource ModernPasswordBox}" PasswordChanged="PasswordBox_PasswordChanged"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,20">
                            <TextBlock Text="Xác nhận mật khẩu mới:" Style="{StaticResource TextBlockNormal}" Margin="0,0,0,6"/>
                            <PasswordBox x:Name="txtConfirmPassword" Style="{StaticResource ModernPasswordBox}" PasswordChanged="PasswordBox_PasswordChanged"/>
                        </StackPanel>

                        <Button Style="{StaticResource PrimaryButton}" Content="Cập nhật mật khẩu" Height="40" Command="{Binding ChangePasswordCommand}"/>
                    </StackPanel>
                </Border>

                <!-- Logout Card -->
                <Border Style="{StaticResource BaseCardStyle}" Padding="24">
                    <StackPanel>
                        <TextBlock Text="Hệ thống" Style="{StaticResource TextBlockBold}" FontSize="18" Margin="0,0,0,16"/>
                        <TextBlock Text="Ngày tham gia hệ thống: " Style="{StaticResource TextBlockNormal}" Margin="0,0,0,20">
                            <Run Text="{Binding JoinedDate}" FontWeight="Bold"/>
                        </TextBlock>
                        <Button Style="{StaticResource DangerButton}" Content="Đăng xuất khỏi tài khoản" Height="40" Command="{Binding LogoutCommand}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
