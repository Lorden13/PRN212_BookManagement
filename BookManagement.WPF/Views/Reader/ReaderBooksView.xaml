<UserControl x:Class="BookManagement.Views.Reader.ReaderBooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:BookManagement.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Filters Card -->
            <RowDefinition Height="*"/>    <!-- Catalog Card Grid & Pagination -->
        </Grid.RowDefinitions>

        <!-- Filters Section -->
        <Border Grid.Row="0" Style="{StaticResource BaseCardStyle}" Padding="16" Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>      <!-- Search & Price -->
                    <ColumnDefinition Width="16"/>     <!-- Gap -->
                    <ColumnDefinition Width="200"/>    <!-- Category & Sort -->
                    <ColumnDefinition Width="16"/>     <!-- Gap -->
                    <ColumnDefinition Width="200"/>    <!-- Rating & Clear -->
                </Grid.ColumnDefinitions>

                <!-- Row 0: Search Box, Category, Rating -->
                <Grid Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">
                    <TextBox Style="{StaticResource ModernTextBox}" 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Tag="Search by title or author..."/>
                    <Path Data="{StaticResource IconSearch}" 
                          Fill="{DynamicResource TextMutedBrush}" 
                          Width="16" Height="16" Stretch="Uniform" 
                          HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,14,0" 
                          IsHitTestVisible="False"/>
                </Grid>

                <ComboBox Grid.Row="0" Grid.Column="2" VerticalAlignment="Center"
                          Style="{StaticResource ModernComboBox}" 
                          ItemsSource="{Binding Categories}" 
                          SelectedItem="{Binding SelectedCategory}"/>

                <ComboBox Grid.Row="0" Grid.Column="4" VerticalAlignment="Center"
                          Style="{StaticResource ModernComboBox}" 
                          ItemsSource="{Binding RatingOptions}" 
                          SelectedItem="{Binding SelectedRatingOption}"/>

                <!-- Row 1: Price Slider, Sort ComboBox, Clear Button -->
                <Grid Grid.Row="1" Grid.Column="0" Margin="0,12,0,0" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" 
                               Text="{Binding MaxPrice, StringFormat='Max Price: ${0:F2}'}" 
                               Width="110" 
                               Style="{StaticResource TextBlockNormal}" 
                               VerticalAlignment="Center"/>
                    <Slider Grid.Column="1" 
                            Minimum="0" Maximum="50" 
                            Value="{Binding MaxPrice}" 
                            TickFrequency="1" 
                            IsSnapToTickEnabled="False"
                            VerticalAlignment="Center"/>
                </Grid>

                <ComboBox Grid.Row="1" Grid.Column="2" Margin="0,12,0,0" VerticalAlignment="Center"
                          Style="{StaticResource ModernComboBox}" 
                          ItemsSource="{Binding SortOptions}" 
                          SelectedItem="{Binding SelectedSortOption}"/>

                <Button Grid.Row="1" Grid.Column="4" Margin="0,12,0,0" VerticalAlignment="Center"
                        Style="{StaticResource SecondaryButton}" 
                        Content="Clear Filters" 
                        Command="{Binding ClearFiltersCommand}"/>
            </Grid>
        </Border>

        <!-- Catalog Card Grid -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <ItemsControl ItemsSource="{Binding PagedBooks}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    
                    </ItemsControl>

                    <!-- Empty State if no books found -->
                    
                </StackPanel>
            </ScrollViewer>
            
            <!-- Pagination -->
            
        </Grid>
    </Grid>
</UserControl>
