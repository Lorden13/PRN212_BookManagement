<UserControl x:Class="BookManagement.Views.Common.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:BookManagement.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Margin="24">
        <Border Style="{StaticResource BaseCardStyle}" Padding="32" Width="450" HorizontalAlignment="Left" VerticalAlignment="Top">
            <StackPanel>
                <controls:SectionTitle Text="Đổi mật khẩu" Margin="0,0,0,24"/>

                <!-- Current Password -->
                <TextBlock Text="Mật khẩu hiện tại *" Style="{StaticResource TextBlockSmallHeader}" Margin="0,0,0,8"/>
                <PasswordBox x:Name="txtCurrentPassword" 
                             Style="{StaticResource ModernPasswordBox}" 
                             PasswordChanged="PasswordBox_PasswordChanged"
                             Margin="0,0,0,20"/>

                <!-- New Password -->
                <TextBlock Text="Mật khẩu mới *" Style="{StaticResource TextBlockSmallHeader}" Margin="0,0,0,8"/>
                <PasswordBox x:Name="txtNewPassword" 
                             Style="{StaticResource ModernPasswordBox}" 
                             PasswordChanged="PasswordBox_PasswordChanged"
                             Margin="0,0,0,20"/>

                <!-- Confirm Password -->
                <TextBlock Text="Xác nhận mật khẩu mới *" Style="{StaticResource TextBlockSmallHeader}" Margin="0,0,0,8"/>
                <PasswordBox x:Name="txtConfirmPassword" 
                             Style="{StaticResource ModernPasswordBox}" 
                             PasswordChanged="PasswordBox_PasswordChanged"
                             Margin="0,0,0,28"/>

                <!-- Submit Button -->
                <Button Style="{StaticResource PrimaryButton}" 
                        Command="{Binding UpdatePasswordCommand}" 
                        Content="Cập nhật mật khẩu" 
                        Height="40" 
                        HorizontalAlignment="Stretch"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
