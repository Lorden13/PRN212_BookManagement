<UserControl x:Class="BookManagement.Views.Author.AuthorApprovalStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:BookManagement.Controls"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Status Summary Cards -->
            <RowDefinition Height="Auto"/> <!-- Search / Filter -->
            <RowDefinition Height="*"/>    <!-- List -->
        </Grid.RowDefinitions>

        <!-- Status Summary Cards -->
        <UniformGrid Grid.Row="0" Columns="4" Margin="-8,0,-8,20">
            <controls:StatisticCard Title="Đang chờ duyệt" Value="{Binding PendingCount}" IconKey="IconClock" TrendText="Chờ Ban kiểm duyệt" TrendBrush="{DynamicResource WarningBrush}"/>
            <controls:StatisticCard Title="Đã phê duyệt" Value="{Binding ApprovedCount}" IconKey="IconCheck" TrendText="Đã xuất bản" TrendBrush="{DynamicResource SuccessBrush}"/>
            <controls:StatisticCard Title="Bị từ chối" Value="{Binding RejectedCount}" IconKey="IconClose" TrendText="Cần chỉnh sửa lại" TrendBrush="{DynamicResource DangerBrush}"/>
            <controls:StatisticCard Title="Bản nháp" Value="{Binding DraftCount}" IconKey="IconBook" TrendText="Chưa gửi duyệt"/>
        </UniformGrid>

        <!-- Search / Filter -->
        <Border Grid.Row="1" Style="{StaticResource BaseCardStyle}" Padding="16" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <TextBox Style="{StaticResource ModernTextBox}"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Tag="Tìm kiếm theo tiêu đề sách hoặc thể loại..."/>
                    <Path Data="{StaticResource IconSearch}"
                          Fill="{DynamicResource TextMutedBrush}"
                          Width="16" Height="16" Stretch="Uniform"
                          HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,14,0"
                          IsHitTestVisible="False"/>
                </Grid>

                <ComboBox Grid.Column="2"
                          Style="{StaticResource ModernComboBox}"
                          ItemsSource="{Binding Statuses}"
                          SelectedItem="{Binding SelectedStatus}"/>
            </Grid>
        </Border>

        <!-- Approval Status List -->
        <Border Grid.Row="2" Style="{StaticResource BaseCardStyle}" Padding="20">
            <Grid>
                <DataGrid ItemsSource="{Binding ItemsView}"
                          Style="{StaticResource ModernDataGrid}">
                    <DataGrid.Columns>
                        <!-- Book Title -->
                        <DataGridTemplateColumn Header="Tên sách" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Title}"
                                               VerticalAlignment="Center"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Category -->
                        <DataGridTextColumn Header="Thể loại" Binding="{Binding Category}" Width="1*"/>

                        <!-- Submitted Date -->
                        <DataGridTextColumn Header="Ngày gửi" Binding="{Binding SubmittedDate}" Width="1.3*"/>

                        <!-- Status Badge -->
                        <DataGridTemplateColumn Header="Trạng thái" Width="1.1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <controls:StatusBadge Content="{Binding Status}" HorizontalAlignment="Left"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Latest Comment -->
                        <DataGridTextColumn Header="Nhận xét mới nhất" Binding="{Binding LatestComment}" Width="3*"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Empty State -->
                <controls:EmptyState>
                    <controls:EmptyState.Style>
                        <Style TargetType="controls:EmptyState">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ItemsView.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </controls:EmptyState.Style>
                </controls:EmptyState>
            </Grid>
        </Border>
    </Grid>
</UserControl>
